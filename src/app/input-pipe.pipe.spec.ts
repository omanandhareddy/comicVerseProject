import { InputPipePipe } from './input-pipe.pipe';

describe('InputPipePipe', () => {
  it('create an instance', () => {
    const pipe = new InputPipePipe();
    expect(pipe).toBeTruthy();
  });
});
